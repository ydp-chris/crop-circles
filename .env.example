# Copy to .env and fill in. Never commit .env.
#
# These map to the existing ValleySomm Supabase. The values already live in
# /home/ydp-admin/agents/.env as SUPABASE_VS_URL / SUPABASE_VS_SERVICE_KEY.
# Keeping the names generic here so this project stays portable.

SUPABASE_URL=https://sodzbrrgcvtznosboznq.supabase.co
SUPABASE_SERVICE_KEY=REPLACE_ME

# Phase 2 LLM extraction (https://console.anthropic.com/)
ANTHROPIC_API_KEY=sk-ant-REPLACE_ME
